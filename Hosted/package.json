{
  "name": "apple-core-hosted",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "npm run build",
    "test": "node --test protocol.test.mjs integration.test.mjs",
    "build": "wrangler deploy --dry-run --outdir build",
    "types": "wrangler types"
  },
  "devDependencies": {
    "miniflare": "5.20260907.0-alpha",
    "wrangler": "4.129.1"
  }
}
