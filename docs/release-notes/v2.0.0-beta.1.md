# Apple Core 2.0 Beta 1

Adds an invitation-based hosted connection at `mcp.applecore.app/mcp`, alongside
the existing option to use your own Cloudflare account and domain. Hosted
customers do not need Cloudflare credentials or a tunnel helper. Onboarding
explains both choices and the requirement to keep the Mac running.

Each Mac has a separate relay identity. Apple Core continues to verify OAuth
PKCE, approve clients, authenticate tools, and enforce enabled-service and
filesystem access on the Mac. The shared endpoint serves the Apple Core icon.

There is no new hosted payment check in this beta. Existing app activation is
unchanged. Beta updates are installed manually, separately from the stable
Sparkle feed and Gumroad download.

Hosted access currently supports Streamable HTTP POST and DELETE, up to four
concurrent requests, 1 MiB requests, and 8 MiB responses. Large captures and
standalone SSE subscriptions are not supported by this beta relay. Switching
from a self-hosted address requires reconnecting OAuth clients.

Published September 8, 2026 as a GitHub prerelease:
https://github.com/oliverames/apple-core/releases/tag/v2.0.0-beta.1
The release targets `53eb167ea7e0bf72798ddb7762db11711c1a5783` and contains the
Developer ID signed, notarized build 28 ZIP. Its uploaded SHA-256 matches:
`087eb4565e4d2f2049482ac16fd7221490d97df309ec7e19d3ddbc42e0bcba7d`.
The stable latest release remains v1.7.3.

Home Server was updated to direct-distribution build 32 on September 8, 2026.
The signed, stapled app passed Gatekeeper on the receiving host, and the actual
Codex connector returned Home Server system information after restart. Hosted
mode remains enabled and the own-Cloudflare mode remains disabled there.
Build 32 includes the bundled third-party notices missing from build 28,
clearer service descriptions in the sidebar interface, and compatibility with
ChatGPT's structured experimental capabilities during MCP initialization.
Its installation ZIP SHA-256 is
`65fddf2b2a291935f5da23333ef2059165c6975c33f5558ba3df30678df2a759`.
Apple notarization accepted submission `4d1ae31c-fc97-4e07-8cea-1246e9a952fc`.
The published Beta 1 asset above remains build 28 and was not replaced.
Codex, ChatGPT Work, and Claude.ai connectivity to Home Server is verified.
ChatGPT and Claude each discovered 128 tools and passed eleven of twelve
read-only service checks. Mail remains unresolved: a fresh Codex call on build
32 returns `APP_NOT_RUNNING`, while ChatGPT reports `INVALID_ARGUMENT`.
These checks establish service access, not exhaustive coverage of every tool.
App Store work is deferred at Oliver's direction. The active Home Server app
remains the direct-distribution version, using `https://mcp.applecore.app/mcp`.
Detailed evidence is in `docs/home-server-acceptance-2026-09-08.md`.
See `Hosted/README.md` for operator details.
